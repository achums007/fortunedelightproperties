<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Securely rewrite all traffic into the /public/ folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
